# Application configuration template.
# Copy to .env for local Node.js development; docker-compose.yml sets its own values.
# Never commit a real .env file — it is git-ignored.

PORT=3000
NODE_ENV=development
# Short git commit SHA baked into images by CI; anything readable is fine locally.
APP_VERSION=local-dev
